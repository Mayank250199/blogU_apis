import React from "react";
import { Link } from "react-router-dom";





const ProfileMain = ({
  username,
  name,
  email
  
}) => (
  
  <div>
    <h1>hii {username}</h1>
    </div>
  );


export default ProfileMain;